<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('include/head.ejs') %>
    <link rel="stylesheet" href="styles/restaurants.css" />
  </head>
  <body>
    <%- include('include/header.ejs') %>
    <%- include('include/side-drawer.ejs') %>
    <main>
      <h1>Recommended restaurants</h1>
      <% if (numberOfInputN === 0) { %>
        <p>운이 없게도, 아무도 값을 입력하지 않았습니다.</p>
      <% } else { %>
        <p>Find your next favorite restaurants with help of our other users!</p>
        <p> 사용자들이 <%= numberOfInputN %>개의 값을 입력함.</p>
        <form action="/restaurants" method="GET">
          <input type="hidden" value=<%= nextOrder %> name="order">
          <button class="btn">순서 변경 버튼</button>
        </form>
        <ul id="restaurants-list">
          <% for (const restaurant of restaurants) { %>
            <%- include('include/restaurants/restaurant-item', { restaurant: restaurant }) %>
          <% } %> <!-- End of loop -->
        </ul>
      <% } %>
    </main>
  </body>
</html>
